{
  "name": "proofbyte",
  "private": true,
  "version": "0.0.1",
  "description": "Technical-evidence engine: auditor-ready artifacts generated from your actual code and runtime",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces"
  },
  "engines": {
    "node": ">=22"
  }
}
